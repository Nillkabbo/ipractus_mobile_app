---
phase: 01-foundation-auth-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app.json
  - tsconfig.json
  - babel.config.js
  - src/constants/colors.ts
  - src/constants/typography.ts
  - src/constants/spacing.ts
  - src/constants/indices.ts
  - src/config/theme.ts
  - src/contexts/ThemeContext.tsx
  - src/hooks/useTheme.ts
  - src/App.tsx
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Expo SDK 52 project initializes successfully on iOS and Android"
    - "All navigation, storage, and image picker dependencies are installed"
    - "Lexend font loads correctly and displays in the app"
    - "Dark theme applies with Stitch design tokens (#137fec primary, dark backgrounds)"
    - "Light theme applies with appropriate contrast colors"
    - "Theme preference persists across app restarts via AsyncStorage"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "expo", "@react-navigation", "expo-secure-store", "@expo-google-fonts/lexend"
    - path: "app.json"
      provides: "Expo app configuration"
      contains: "expo.sdk: 52.0.0"
    - path: "src/config/theme.ts"
      provides: "Theme definitions (light/dark) with Stitch tokens"
      exports: ["lightTheme", "darkTheme"]
    - path: "src/contexts/ThemeContext.tsx"
      provides: "Theme state management and toggle"
      exports: ["ThemeProvider", "useTheme"]
    - path: "src/constants/colors.ts"
      provides: "Color tokens from Stitch designs"
      contains: "#137fec"
  key_links:
    - from: "src/App.tsx"
      to: "src/contexts/ThemeContext.tsx"
      via: "ThemeProvider wrapper"
      pattern: "ThemeProvider"
    - from: "src/App.tsx"
      to: "@expo-google-fonts/lexend"
      via: "useFonts hook"
      pattern: "useFonts.*Lexend"
    - from: "src/contexts/ThemeContext.tsx"
      to: "src/config/theme.ts"
      via: "import"
      pattern: "from.*config/theme"
---

<objective>
Initialize React Native Expo project with SDK 52, install required dependencies for navigation, storage, and authentication, configure app permissions, and establish the theme system with Stitch design tokens and Lexend font loading.

**Purpose:** This plan creates the foundation for the entire app - the Expo project, all required dependencies, and the theme infrastructure that all screens will use for consistent styling.

**Output:** Working Expo app with theme system supporting dark/light modes, Lexend font, and proper permissions configuration.
</objective>

<execution_context>
@/Users/md.rakibulhasan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/md.rakibulhasan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth-navigation/01-CONTEXT.md
@.planning/phases/01-foundation-auth-navigation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Expo SDK 52 project and install dependencies</name>
  <files>package.json, app.json, tsconfig.json, babel.config.js, .gitignore</files>
  <action>
Create Expo project using:
```bash
npx create-expo-app@latest . --template blank-typescript
```

Accept defaults. This creates package.json, app.json, tsconfig.json, babel.config.js.

Then install ALL dependencies for Phase 1 (per RESEARCH.md standard stack):
```bash
# Core navigation
npm install @react-navigation/native
npx expo install react-native-screens react-native-safe-area-context

# Navigators
npm install @react-navigation/native-stack @react-navigation/bottom-tabs

# Storage and auth
npx expo install expo-secure-store
npx expo install @react-native-async-storage/async-storage
npx expo install expo-local-authentication

# Image handling
npx expo install expo-image-picker

# Fonts and icons
npm install @expo-google-fonts/lexend expo-font
npm install @expo/vector-icons

# Forms (optional but recommended per research)
npm install react-hook-form zod @hookform/resolvers
```

Update app.json with Expo SDK 52 and permissions (iOS Face ID, camera, photo library):
```json
{
  "expo": {
    "sdkVersion": "52.0.0",
    "name": "iPractus",
    "slug": "ipractus",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "automatic",
    "splash": {
      "image": "./assets/splash.png",
      "resizeMode": "contain",
      "backgroundColor": "#000000"
    },
    "ios": {
      "bundleIdentifier": "com.ipractus.app",
      "infoPlist": {
        "NSFaceIDUsageDescription": "Use Face ID to quickly sign in to your iPractus account.",
        "NSCameraUsageDescription": "Allow iPractus to access your camera to take profile photos.",
        "NSPhotoLibraryUsageDescription": "Allow iPractus to access your photo library to upload profile photos."
      }
    },
    "android": {
      "package": "com.ipractus.app",
      "permissions": [
        "USE_BIOMETRIC",
        "USE_FINGERPRINT",
        "CAMERA",
        "READ_EXTERNAL_STORAGE",
        "WRITE_EXTERNAL_STORAGE"
      ]
    }
  }
}
```

Update .gitignore to add standard Expo ignores (node_modules, .expo, dist, etc.) if not present.

DO NOT initialize React Native Paper (mentioned in old roadmap) - use custom components with theme system instead.
  </action>
  <verify>
```bash
# Verify Expo project initialized
cat app.json | grep "sdkVersion.*52"

# Verify all dependencies installed
cat package.json | grep -E "@react-navigation|expo-secure-store|expo-local-authentication|expo-image-picker|@expo-google-fonts/lexend|@expo/vector-icons"

# Run the project to verify it works
npx expo start
```
Press Ctrl+C after verification.
  </verify>
  <done>
Expo SDK 52 project initializes successfully, all dependencies are installed in package.json, app.json contains proper permissions for Face ID, camera, and photo library access.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create folder structure and constant files with Stitch design tokens</name>
  <files>src/constants/colors.ts, src/constants/typography.ts, src/constants/spacing.ts, src/constants/indices.ts</files>
  <action>
Create the following folder structure (per RESEARCH.md recommended structure):
```bash
mkdir -p src/{components,config,constants,contexts,hooks,navigators,screens/{auth,profile,dashboard},services/{auth,storage},types,utils}
```

Create src/constants/colors.ts with Stitch design tokens from CONTEXT.md:
```typescript
// Design tokens from Google Stitch project
export const colors = {
  // Primary brand color
  primary: '#137fec',

  // Dark theme colors (default)
  background: '#000000',
  surface: '#1a1a1a',
  surfaceVariant: '#2a2a2a',
  text: '#ffffff',
  textSecondary: '#a0a0a0',
  textDisabled: '#666666',
  border: '#333333',
  error: '#ff4444',
  success: '#00cc66',
  warning: '#ffaa00',
  info: '#137fec',

  // Light theme colors
  light: {
    background: '#ffffff',
    surface: '#f5f5f5',
    surfaceVariant: '#e8e8e8',
    text: '#000000',
    textSecondary: '#666666',
    textDisabled: '#a0a0a0',
    border: '#e0e0e0',
  },
} as const;
```

Create src/constants/typography.ts:
```typescript
export const typography = {
  fontFamily: 'Lexend',

  fontSize: {
    xs: 12,
    sm: 14,
    md: 16,
    lg: 18,
    xl: 20,
    xxl: 24,
    xxxl: 32,
  },

  fontWeight: {
    regular: '400' as const,
    medium: '500' as const,
    semibold: '600' as const,
    bold: '700' as const,
  },

  lineHeight: {
    tight: 1.2,
    normal: 1.5,
    relaxed: 1.8,
  },
} as const;
```

Create src/constants/spacing.ts:
```typescript
export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
} as const;
```

Create src/constants/indices.ts:
```typescript
export const zIndex = {
  toast: 1000,
  modal: 900,
  dropdown: 800,
  header: 700,
} as const;

export const borderRadius = {
  sm: 4,
  md: 8,  // Default from Stitch
  lg: 16,
  xl: 24,
  round: 9999,
} as const;
```

DO NOT use a separate constants file for route names - those will be defined in navigation config.
  </action>
  <verify>
```bash
# Verify all constant files exist
ls -la src/constants/
cat src/constants/colors.ts | grep "#137fec"
cat src/constants/typography.ts | grep "Lexend"
```
  </verify>
  <done>
All constant files exist with Stitch design tokens: #137fec primary color, Lexend font family, 8px border radius, dark theme colors, and spacing scale.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create theme system with ThemeContext and Lexend font loading</name>
  <files>src/config/theme.ts, src/contexts/ThemeContext.tsx, src/hooks/useTheme.ts, src/App.tsx</files>
  <action>
Create src/config/theme.ts with React Navigation theme objects using Stitch tokens:
```typescript
import { DefaultTheme, DarkTheme } from '@react-navigation/native';
import { colors } from '../constants/colors';

export const lightTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: colors.primary,
    background: colors.light.background,
    card: colors.light.surface,
    text: colors.light.text,
    border: colors.light.border,
    notification: colors.primary,
  },
};

export const darkTheme = {
  ...DarkTheme,
  colors: {
    ...DarkTheme.colors,
    primary: colors.primary,
    background: colors.background,
    card: colors.surface,
    text: colors.text,
    border: colors.border,
    notification: colors.primary,
  },
};
```

Create src/contexts/ThemeContext.tsx (pattern from RESEARCH.md):
```typescript
import React, { createContext, useContext, useState, useEffect } from 'react';
import { useColorScheme } from 'react-native';
import { DefaultTheme } from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { lightTheme, darkTheme } from '../config/theme';

type ThemeMode = 'light' | 'dark' | 'system';

interface ThemeContextType {
  themeMode: ThemeMode;
  isDark: boolean;
  setThemeMode: (mode: ThemeMode) => void;
  theme: typeof DefaultTheme;
}

const ThemeContext = createContext<ThemeContextType | null>(null);

export const ThemeProvider = ({ children }: { children: React.ReactNode }) => {
  const systemScheme = useColorScheme();
  const [themeMode, setThemeModeState] = useState<ThemeMode>('system');

  useEffect(() => {
    // Load saved theme preference
    AsyncStorage.getItem('themeMode').then(saved => {
      if (saved) setThemeModeState(saved as ThemeMode);
    });
  }, []);

  const setThemeMode = async (mode: ThemeMode) => {
    setThemeModeState(mode);
    await AsyncStorage.setItem('themeMode', mode);
  };

  const isDark = themeMode === 'system'
    ? systemScheme === 'dark'
    : themeMode === 'dark';

  const theme = isDark ? darkTheme : lightTheme;

  return (
    <ThemeContext.Provider value={{ themeMode, isDark, setThemeMode, theme }}>
      {children}
    </ThemeContext.Provider>
  );
};

export const useTheme = () => {
  const context = useContext(ThemeContext);
  if (!context) {
    throw new Error('useTheme must be used within ThemeProvider');
  }
  return context;
};
```

Create src/hooks/useTheme.ts:
```typescript
export { useTheme, ThemeProvider } from '../contexts/ThemeContext';
export type { ThemeMode } from '../contexts/ThemeContext';
```

Update src/App.tsx to integrate theme and font loading:
```typescript
import React from 'react';
import { StatusBar } from 'expo-status-bar';
import { NavigationContainer } from '@react-navigation/native';
import { useFonts } from 'expo-font';
import { Lexend_400Regular } from '@expo-google-fonts/lexend';
import { ThemeProvider, useTheme } from './contexts/ThemeContext';
import { RootNavigator } from './navigators/RootNavigator'; // Will create in plan 01-03

// App wrapper with theme provider
export default function App() {
  const [fontsLoaded] = useFonts({
    Lexend_400Regular,
  });

  if (!fontsLoaded) {
    return null; // Or loading screen
  }

  return (
    <ThemeProvider>
      <AppContent />
    </ThemeProvider>
  );
}

function AppContent() {
  return (
    <>
      <StatusBar style="auto" />
      <RootNavigator />
    </>
  );
}
```

Note: RootNavigator will be created in plan 01-03, and it will create the NavigationContainer there (not here). For now, this file will have an import error - that's expected.
  </action>
  <verify>
```bash
# Verify theme files exist and have correct structure
cat src/config/theme.ts | grep -E "lightTheme|darkTheme"
cat src/contexts/ThemeContext.tsx | grep "ThemeMode"
cat src/App.tsx | grep "ThemeProvider"
```
  </verify>
  <done>
Theme system is fully configured with ThemeContext, useTheme hook, theme objects using Stitch tokens, and Lexend font loading in App.tsx. Theme preference persists via AsyncStorage.
  </done>
</task>

</tasks>

<verification>
Overall phase verification:
1. Run `npx expo start` and verify the app launches without errors (RootNavigator import error is expected at this stage)
2. Verify app.json has Expo SDK 52 and all required permissions
3. Verify all dependencies are installed in package.json
4. Verify theme system files exist with correct Stitch tokens
5. Check that folder structure matches recommended pattern from research
</verification>

<success_criteria>
1. Expo SDK 52 project initializes successfully on both iOS and Android simulators
2. All navigation, storage, authentication, image picker, and font dependencies are installed
3. App.json contains proper iOS and Android permissions for Face ID, camera, and photo library
4. Lexend font loads via useFonts hook
5. Theme system supports light/dark/system modes with Stitch design tokens (#137fec primary)
6. Theme preference persists across app restarts via AsyncStorage
7. Folder structure follows recommended pattern from research
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth-navigation/01-01-SUMMARY.md`
</output>

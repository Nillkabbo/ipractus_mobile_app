export const zIndex = {
  toast: 1000,
  modal: 900,
  dropdown: 800,
  header: 700,
} as const;

export const borderRadius = {
  sm: 4,
  md: 8,  // Default from Stitch
  lg: 16,
  xl: 24,
  round: 9999,
} as const;
